httplib2==0.12.0
SQLAlchemy==1.1.11
PyWavelets==0.5.2
obspy==1.1.0
scipy==0.19.1
spectrum==0.6.5
numpy==1.15.4
matplotlib==2.0.2
PyMySQL==0.9.2
tensorflow_gpu==1.12.0
pywt==1.0.6


版本高亦可

除了以上必须包，还需要以下操作：

# apt-get install python3-tk
关于ubuntu安装pywt：
先pip3 uninstall pywt，再 apt-get install python3-pywt
安装hyposat依赖环境：

而且如果在win下，用Linux子系统，要怎么调试
跟在window下面是一样的
只是中间location的程序是在linux子系统上执行的而已

